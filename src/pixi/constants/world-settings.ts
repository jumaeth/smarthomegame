export const TILE_SIZE = 16;
export const COLS = 18;
export const ROWS = 10;

export const GAME_WIDTH = TILE_SIZE * COLS;
export const GAME_HEIGHT = TILE_SIZE * ROWS;

export const OFFSET_X = 0;
export const OFFSET_Y = (TILE_SIZE/4) * 3;

export const DEFAULT_POS_X = TILE_SIZE * 9;
export const DEFAULT_POS_Y = TILE_SIZE * 7;

export const MOVE_SPEED = 0.04;

export const ANIMATION_SPEED = 0.2;

export const ZOOM = 10;
