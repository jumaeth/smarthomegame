end2end-job:
  stage: end2end
  rules:
    - if: '$CI_COMMIT_BRANCH == $TEST_BRANCH_NAME'
  script:
    - echo "[JOB] End-to-end tests"
    - echo "[NOT IMPLEMENTED] End-to-End tests using Selenium"

deploy-job:
  stage: deploy
  environment:
    name: testing
  rules:
    - if: '$CI_COMMIT_BRANCH == $TEST_BRANCH_NAME'
  dependency:
    - end2end-job
  script:
    - echo "[JOB] Deploy"
    - echo "[NOT IMPLEMENTED] Deploy to test-environment"
