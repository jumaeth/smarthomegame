services:
  web-dev:
    image: ${CI_REGISTRY_IMAGE}:app_dev-latest
    ports: ["8090:80"]
    profiles: ["dev"]
    restart: unless-stopped

  web-prod:
    image: ${CI_REGISTRY_IMAGE}:app_prod-latest
    ports: ["8080:80"]
    profiles: ["prod"]
    restart: unless-stopped